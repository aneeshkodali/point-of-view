import React from 'react';

const PlayerMatch = props => {

    // get match_suffix
    const match_suffix = props.match.params.match_suffix;

    

    return (
        <div>
            PLAYER MATCH PAGE
        </div>
    );
}

export default PlayerMatch;